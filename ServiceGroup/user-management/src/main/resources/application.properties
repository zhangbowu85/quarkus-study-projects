quarkus.http.host=0.0.0.0
quarkus.http.port=8081

# datasource configuration
quarkus.datasource.db-kind = postgresql
quarkus.datasource.username = postgres
quarkus.datasource.password = Admin+86
quarkus.datasource.reactive.url = vertx-reactive:postgresql://localhost:5432/mytest
# jdbc:postgresql://localhost:5432/mytest

quarkus.redisson.single-server-config.address=redis://localhost:6379
quarkus.redisson.single-server-config.password=null
quarkus.redisson.threads=16
quarkus.redisson.netty-threads=32


# drop and create the database at startup (use `update` to only update the schema)
quarkus.hibernate-orm.database.generation=update
#quarkus.hibernate-orm.sql-load-script=schema/create_course.sql
